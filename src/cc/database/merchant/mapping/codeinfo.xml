<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.database.merchant.mapping.codeinfo">
    <select id="getcodeinfo" parameterType="java.util.HashMap" resultType="java.util.HashMap">
        SELECT * FROM codeinfo where   cid=#{cid}
    </select>

    <select id="insertcodeinfo" parameterType="java.util.HashMap" resultType="java.util.HashMap">
        insert into  codeinfo  ( openid,cardid) values (#{openid} ,#{cid})
    </select>

</mapper>